/* eslint-disable */
// import '../../dropdown.js';

beforeEach('Setting up the userList', function(){
  console.log('beforeEach');
});

describe('Array2', () => {
  describe('#indexOf()2', () => {
    it('should return -1 when the value is not present', async () => {

      // const component = fixture('<seed-dropdown></seed-dropdown>');
      // const component = {};
      // expect(component.not.to.be.null);

      assert.equal(-1, [1, 2, 3].indexOf(4));
    });
  });
});
